name: 🛡️ Deploy Security Infrastructure (cert-manager + WAF)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - prd
          - stg
        default: prd
      install_cert_manager:
        description: 'Install cert-manager for automatic HTTPS'
        required: true
        type: boolean
        default: true
      letsencrypt_email:
        description: 'Email for Let''s Encrypt notifications'
        required: true
        default: 'admin@example.com'
      domain_name:
        description: 'Domain name for HTTPS certificate (e.g., dx02.no-ip.org)'
        required: true
        default: 'dx02.example.com'
      use_letsencrypt_prod:
        description: 'Use Let''s Encrypt Production (false = staging)'
        required: true
        type: boolean
        default: false
      deploy_app_gateway:
        description: 'Deploy Azure Application Gateway with WAF'
        required: true
        type: boolean
        default: false
      app_gateway_sku:
        description: 'Application Gateway SKU'
        required: true
        type: choice
        options:
          - WAF_v2
          - Standard_v2
        default: WAF_v2
      waf_mode:
        description: 'WAF Mode'
        required: true
        type: choice
        options:
          - Prevention
          - Detection
        default: Detection

permissions:
  contents: read
  id-token: write

env:
  AZURE_RESOURCE_GROUP: tx02-${{ github.event.inputs.environment }}-rg
  AKS_CLUSTER_NAME: tx02-${{ github.event.inputs.environment }}-aks
  LOCATION: eastus
  APP_GATEWAY_NAME: tx02-${{ github.event.inputs.environment }}-appgw
  VNET_NAME: tx02-${{ github.event.inputs.environment }}-vnet
  APP_GATEWAY_SUBNET: tx02-${{ github.event.inputs.environment }}-subnet-appgw

jobs:
  deploy-cert-manager:
    name: 📜 Deploy cert-manager
    runs-on: ubuntu-latest
    if: github.event.inputs.install_cert_manager == 'true'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🎯 Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --overwrite-existing

      - name: 🔍 Check if cert-manager is already installed
        id: check-cert-manager
        continue-on-error: true
        run: |
          if kubectl get namespace cert-manager &> /dev/null; then
            echo "installed=true" >> $GITHUB_OUTPUT
            echo "✅ cert-manager já está instalado"
          else
            echo "installed=false" >> $GITHUB_OUTPUT
            echo "📦 cert-manager não está instalado"
          fi

      - name: 📦 Install cert-manager using Helm
        if: steps.check-cert-manager.outputs.installed == 'false'
        run: |
          echo "📦 Installing cert-manager..."
          
          # Add Jetstack Helm repository
          helm repo add jetstack https://charts.jetstack.io
          helm repo update
          
          # Install cert-manager with CRDs
          helm install cert-manager jetstack/cert-manager \
            --namespace cert-manager \
            --create-namespace \
            --version v1.14.0 \
            --set installCRDs=true \
            --set global.leaderElection.namespace=cert-manager \
            --wait --timeout=5m
          
          echo "✅ cert-manager installed successfully"

      - name: ⏳ Wait for cert-manager to be ready
        run: |
          echo "⏳ Waiting for cert-manager pods to be ready..."
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance=cert-manager -n cert-manager --timeout=300s
          echo "✅ cert-manager is ready"

      - name: 🔑 Create ClusterIssuer for Let's Encrypt
        run: |
          LETSENCRYPT_SERVER="${{ github.event.inputs.use_letsencrypt_prod == 'true' && 'https://acme-v02.api.letsencrypt.org/directory' || 'https://acme-staging-v02.api.letsencrypt.org/directory' }}"
          ISSUER_NAME="${{ github.event.inputs.use_letsencrypt_prod == 'true' && 'letsencrypt-prod' || 'letsencrypt-staging' }}"
          
          echo "🔑 Creating ClusterIssuer: $ISSUER_NAME"
          echo "📧 Email: ${{ github.event.inputs.letsencrypt_email }}"
          echo "🌐 Server: $LETSENCRYPT_SERVER"
          
          cat <<EOF | kubectl apply -f -
          apiVersion: cert-manager.io/v1
          kind: ClusterIssuer
          metadata:
            name: $ISSUER_NAME
          spec:
            acme:
              server: $LETSENCRYPT_SERVER
              email: ${{ github.event.inputs.letsencrypt_email }}
              privateKeySecretRef:
                name: $ISSUER_NAME-account-key
              solvers:
              - http01:
                  ingress:
                    class: nginx
          EOF
          
          echo "✅ ClusterIssuer created: $ISSUER_NAME"

      - name: 🎫 Create sample certificate for testing
        run: |
          ISSUER_NAME="${{ github.event.inputs.use_letsencrypt_prod == 'true' && 'letsencrypt-prod' || 'letsencrypt-staging' }}"
          
          echo "🎫 Creating test certificate in dx02 namespace..."
          
          cat <<EOF | kubectl apply -f -
          apiVersion: cert-manager.io/v1
          kind: Certificate
          metadata:
            name: dx02-tls-cert
            namespace: dx02
          spec:
            secretName: dx02-tls-secret
            issuerRef:
              name: $ISSUER_NAME
              kind: ClusterIssuer
            dnsNames:
            - ${{ github.event.inputs.domain_name }}
          EOF
          
          echo "✅ Certificate created for domain: ${{ github.event.inputs.domain_name }}"
          echo "⚠️  Make sure DNS points to: 48.194.53.219"

      - name: 📊 Verify cert-manager installation
        run: |
          echo "======================================"
          echo "📜 cert-manager Installation Summary"
          echo "======================================"
          echo ""
          
          echo "📦 cert-manager Pods:"
          kubectl get pods -n cert-manager
          echo ""
          
          echo "🔑 ClusterIssuers:"
          kubectl get clusterissuer
          echo ""
          
          echo "🎫 Certificates:"
          kubectl get certificate --all-namespaces
          echo ""
          
          echo "✅ cert-manager is ready to issue certificates!"

  deploy-application-gateway:
    name: 🛡️ Deploy Application Gateway with WAF
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_app_gateway == 'true'
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🎯 Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --overwrite-existing

      - name: 🔍 Check if Application Gateway exists
        id: check-appgw
        continue-on-error: true
        run: |
          if az network application-gateway show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }} &> /dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Application Gateway já existe"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "📦 Application Gateway não existe"
          fi

      - name: 🌐 Verify subnet for Application Gateway
        if: steps.check-appgw.outputs.exists == 'false'
        run: |
          echo "🌐 Verifying subnet for Application Gateway..."
          
          # Check if subnet already exists
          if az network vnet subnet show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --vnet-name ${{ env.VNET_NAME }} \
            --name ${{ env.APP_GATEWAY_SUBNET }} &> /dev/null; then
            echo "✅ Subnet '${{ env.APP_GATEWAY_SUBNET }}' already exists"
            
            SUBNET_PREFIX=$(az network vnet subnet show \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --vnet-name ${{ env.VNET_NAME }} \
              --name ${{ env.APP_GATEWAY_SUBNET }} \
              --query addressPrefix -o tsv)
            echo "   Address prefix: $SUBNET_PREFIX"
          else
            echo "❌ Subnet does not exist. Creating with address prefix 10.0.5.0/24..."
            az network vnet subnet create \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --vnet-name ${{ env.VNET_NAME }} \
              --name ${{ env.APP_GATEWAY_SUBNET }} \
              --address-prefixes 10.0.5.0/24
            
            echo "✅ Subnet created"
          fi

      - name: 🌐 Create Public IP for Application Gateway
        id: get-ip
        if: steps.check-appgw.outputs.exists == 'false'
        run: |
          echo "🌐 Creating Public IP for Application Gateway..."
          
          # Create new Public IP for Application Gateway
          az network public-ip create \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-pip \
            --location ${{ env.LOCATION }} \
            --allocation-method Static \
            --sku Standard \
            --tags Environment=${{ github.event.inputs.environment }} ManagedBy=GitHub-Actions
          
          IP_ADDRESS=$(az network public-ip show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-pip \
            --query ipAddress -o tsv)
          
          echo "ip_address=$IP_ADDRESS" >> $GITHUB_OUTPUT
          echo "ip_name=${{ env.APP_GATEWAY_NAME }}-pip" >> $GITHUB_OUTPUT
          
          echo "✅ Public IP created:"
          echo "   Name: ${{ env.APP_GATEWAY_NAME }}-pip"
          echo "   IP: $IP_ADDRESS"
          echo ""
          echo "⚠️  IMPORTANTE: Atualize seu DNS dx02.ddns.net para este novo IP: $IP_ADDRESS"

      - name: 🛡️ Create WAF Policy (before Application Gateway)
        if: steps.check-appgw.outputs.exists == 'false' && github.event.inputs.app_gateway_sku == 'WAF_v2'
        run: |
          echo "🛡️ Creating WAF policy..."
          
          # Create WAF policy
          az network application-gateway waf-policy create \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
            --location ${{ env.LOCATION }}
          
          # Configure WAF settings
          az network application-gateway waf-policy policy-setting update \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --policy-name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
            --mode ${{ github.event.inputs.waf_mode }} \
            --state Enabled \
            --max-request-body-size-in-kb 128 \
            --file-upload-limit-in-mb 100
          
          echo "✅ WAF policy created"

      - name: 🛡️ Create Application Gateway with WAF
        if: steps.check-appgw.outputs.exists == 'false'
        run: |
          echo "🛡️ Creating Application Gateway with WAF..."
          echo "SKU: ${{ github.event.inputs.app_gateway_sku }}"
          echo "WAF Mode: ${{ github.event.inputs.waf_mode }}"
          
          # Get subnet ID
          SUBNET_ID=$(az network vnet subnet show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --vnet-name ${{ env.VNET_NAME }} \
            --name ${{ env.APP_GATEWAY_SUBNET }} \
            --query id -o tsv)
          
          # Use Public IP created in previous step
          echo "🔗 Using Public IP: ${{ steps.get-ip.outputs.ip_address }}"
          PUBLIC_IP_NAME="${{ steps.get-ip.outputs.ip_name }}"
          
          # Get Public IP ID
          PUBLIC_IP_ID=$(az network public-ip show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name $PUBLIC_IP_NAME \
            --query id -o tsv)
          
          # Get WAF Policy ID if SKU is WAF_v2
          if [ "${{ github.event.inputs.app_gateway_sku }}" == "WAF_v2" ]; then
            WAF_POLICY_ID=$(az network application-gateway waf-policy show \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
              --query id -o tsv)
            echo "🛡️ Using WAF Policy: $WAF_POLICY_ID"
            WAF_POLICY_ARG="--waf-policy $WAF_POLICY_ID"
          else
            WAF_POLICY_ARG=""
          fi
          
          # Create Application Gateway
          az network application-gateway create \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }} \
            --location ${{ env.LOCATION }} \
            --sku ${{ github.event.inputs.app_gateway_sku }} \
            --capacity 2 \
            --vnet-name ${{ env.VNET_NAME }} \
            --subnet $SUBNET_ID \
            --public-ip-address $PUBLIC_IP_ID \
            --http-settings-cookie-based-affinity Disabled \
            --frontend-port 80 \
            --http-settings-port 80 \
            --http-settings-protocol Http \
            --priority 100 \
            $WAF_POLICY_ARG \
            --tags Environment=${{ github.event.inputs.environment }} ManagedBy=GitHub-Actions
          
          echo "✅ Application Gateway created"

      - name: 🛡️ Verify WAF Policy association
        if: github.event.inputs.app_gateway_sku == 'WAF_v2'
        run: |
          echo "🛡️ Configuring WAF policy..."
          
          # Create WAF policy
          az network application-gateway waf-policy create \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
            --location ${{ env.LOCATION }}
          
          # Configure WAF settings
          az network application-gateway waf-policy policy-setting update \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --policy-name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
            --mode ${{ github.event.inputs.waf_mode }} \
            --state Enabled \
            --max-request-body-size-in-kb 128 \
            --file-upload-limit-in-mb 100
          
          # Associate WAF policy with Application Gateway
          az network application-gateway update \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }} \
            --set firewallPolicy.id=$(az network application-gateway waf-policy show \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
              --query id -o tsv)
          
          echo "✅ WAF policy configured"

      - name: 🔧 Install AGIC (Application Gateway Ingress Controller)
        run: |
          echo "🔧 Installing AGIC..."
          
          # Enable AGIC add-on on AKS
          az aks enable-addons \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --addons ingress-appgw \
            --appgw-id $(az network application-gateway show \
              --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
              --name ${{ env.APP_GATEWAY_NAME }} \
              --query id -o tsv) || echo "⚠️ AGIC may already be enabled"
          
          echo "✅ AGIC configured"

      - name: 📊 Verify Application Gateway installation
        run: |
          echo "======================================"
          echo "🛡️ Application Gateway Summary"
          echo "======================================"
          echo ""
          
          echo "🌐 Application Gateway:"
          az network application-gateway show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }} \
            --query "{name:name,sku:sku.name,tier:sku.tier,capacity:sku.capacity,provisioningState:provisioningState}" \
            -o table
          echo ""
          
          echo "🌐 Public IP (novo):"
          APPGW_IP=$(az network public-ip show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-pip \
            --query ipAddress -o tsv)
          echo "IP Address: $APPGW_IP"
          echo ""
          echo "⚠️  IMPORTANTE: Atualize seu DNS!"
          echo "   dx02.ddns.net → $APPGW_IP"
          echo ""
          
          echo "🛡️ WAF Policy:"
          az network application-gateway waf-policy show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.APP_GATEWAY_NAME }}-waf-policy \
            --query "{name:name,mode:policySettings.mode,state:policySettings.state}" \
            -o table 2>/dev/null || echo "WAF policy not configured"
          echo ""
          
          echo "✅ Application Gateway is ready!"

  configure-ingress:
    name: 🔗 Configure Ingress with HTTPS
    runs-on: ubuntu-latest
    needs: [deploy-cert-manager, deploy-application-gateway]
    if: |
      always() && 
      (needs.deploy-cert-manager.result == 'success' || needs.deploy-cert-manager.result == 'skipped') &&
      (needs.deploy-application-gateway.result == 'success' || needs.deploy-application-gateway.result == 'skipped')
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🎯 Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --overwrite-existing

      - name: 🔗 Update DX02 Ingress with HTTPS and cert-manager
        if: github.event.inputs.install_cert_manager == 'true'
        run: |
          ISSUER_NAME="${{ github.event.inputs.use_letsencrypt_prod == 'true' && 'letsencrypt-prod' || 'letsencrypt-staging' }}"
          
          echo "🔗 Updating DX02 Ingress with HTTPS support..."
          echo "Domain: ${{ github.event.inputs.domain_name }}"
          
          cat <<EOF | kubectl apply -f -
          apiVersion: networking.k8s.io/v1
          kind: Ingress
          metadata:
            name: dx02
            namespace: dx02
            annotations:
              cert-manager.io/cluster-issuer: $ISSUER_NAME
              nginx.ingress.kubernetes.io/ssl-redirect: "true"
              nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
              nginx.ingress.kubernetes.io/proxy-body-size: "10m"
              nginx.ingress.kubernetes.io/rate-limit: "100"
          spec:
            ingressClassName: nginx
            tls:
            - hosts:
              - ${{ github.event.inputs.domain_name }}
              secretName: dx02-tls-secret
            rules:
            - host: ${{ github.event.inputs.domain_name }}
              http:
                paths:
                - path: /
                  pathType: Prefix
                  backend:
                    service:
                      name: dx02-service
                      port:
                        number: 80
          EOF
          
          echo "✅ Ingress updated with HTTPS for domain: ${{ github.event.inputs.domain_name }}"

      - name: 📊 Display final summary
        run: |
          echo "======================================"
          echo "🎉 Security Infrastructure Deployed!"
          echo "======================================"
          echo ""
          
          if [[ "${{ github.event.inputs.install_cert_manager }}" == "true" ]]; then
            echo "📜 cert-manager: ✅ Installed"
            ISSUER_NAME="${{ github.event.inputs.use_letsencrypt_prod == 'true' && 'letsencrypt-prod' || 'letsencrypt-staging' }}"
            echo "   ClusterIssuer: $ISSUER_NAME"
            echo "   Email: ${{ github.event.inputs.letsencrypt_email }}"
            echo "   Domain: ${{ github.event.inputs.domain_name }}"
          fi
          
          if [[ "${{ github.event.inputs.deploy_app_gateway }}" == "true" ]]; then
            echo ""
            echo "🛡️ Application Gateway: ✅ Deployed"
            echo "   SKU: ${{ github.event.inputs.app_gateway_sku }}"
            echo "   WAF Mode: ${{ github.event.inputs.waf_mode }}"
            echo "   Public IP: 48.194.53.219 (reused from AKS)"
          fi
          
          echo ""
          echo "======================================"
          echo "📝 IMPORTANT - Configure DNS NOW:"
          echo "======================================"
          echo ""
          echo "1️⃣ Go to no-ip.com"
          echo "2️⃣ Create/Update hostname: ${{ github.event.inputs.domain_name }}"
          echo "3️⃣ Point to IP: 48.194.53.219"
          echo ""
          echo "4️⃣ Wait 5-10 minutes for cert-manager to issue certificate"
          echo "5️⃣ Access: https://${{ github.event.inputs.domain_name }}"
          echo ""
          echo "✅ Deployment complete!"
