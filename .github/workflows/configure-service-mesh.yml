name: 🕸️ Configure Azure Service Mesh (Istio)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy'
        required: true
        type: choice
        options:
          - prd
          - stg
        default: prd
      enable_mtls:
        description: 'Enable mTLS'
        required: true
        type: boolean
        default: true
      enable_telemetry:
        description: 'Enable Telemetry'
        required: true
        type: boolean
        default: true
      enable_ingress_gateway:
        description: 'Enable Ingress Gateway'
        required: true
        type: boolean
        default: true

permissions:
  contents: read
  id-token: write

env:
  AZURE_RESOURCE_GROUP: tx02-${{ github.event.inputs.environment }}-rg
  AKS_CLUSTER_NAME: tx02-${{ github.event.inputs.environment }}-aks
  ISTIO_VERSION: "1.20"

jobs:
  install-service-mesh:
    name: 🕸️ Install Istio Service Mesh
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔐 Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🎯 Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --overwrite-existing

      - name: 📦 Check if Istio add-on is already enabled
        id: check-istio
        run: |
          ISTIO_ENABLED=$(az aks show \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --query 'serviceMeshProfile.mode' -o tsv)
          
          echo "istio_enabled=$ISTIO_ENABLED" >> $GITHUB_OUTPUT
          echo "Current Service Mesh status: $ISTIO_ENABLED"

      - name: 🚀 Enable Istio add-on on AKS
        if: steps.check-istio.outputs.istio_enabled != 'Istio'
        run: |
          echo "🕸️ Enabling Istio add-on on AKS cluster..."
          az aks mesh enable \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }}
          
          echo "⏳ Waiting for Istio add-on to be ready (this may take 5-10 minutes)..."
          sleep 60
          
          # Wait for Istio pods with retries
          for i in {1..20}; do
            echo "Attempt $i/20: Checking Istio pods..."
            READY_PODS=$(kubectl get pods -n aks-istio-system --field-selector=status.phase=Running 2>/dev/null | tail -n +2 | wc -l)
            if [ "$READY_PODS" -gt 0 ]; then
              echo "✅ Istio pods are running!"
              kubectl get pods -n aks-istio-system
              break
            fi
            echo "Waiting... ($i/20)"
            sleep 30
          done

      - name: 🔍 Verify Istio installation
        run: |
          echo "📊 Checking Istio components..."
          kubectl get pods -n aks-istio-system
          kubectl get svc -n aks-istio-system
          
          echo ""
          echo "📋 Istio version:"
          kubectl get pods -n aks-istio-system -o jsonpath='{.items[0].spec.containers[0].image}'

      - name: 🏷️ Enable Istio injection for namespaces
        run: |
          echo "🏷️ Enabling Istio sidecar injection for key namespaces..."
          
          # Create namespaces if they don't exist
          kubectl create namespace dx02 --dry-run=client -o yaml | kubectl apply -f -
          kubectl create namespace monitoring --dry-run=client -o yaml | kubectl apply -f -
          
          # Enable Istio injection
          kubectl label namespace dx02 istio-injection=enabled --overwrite
          kubectl label namespace monitoring istio-injection=enabled --overwrite
          
          echo ""
          echo "✅ Namespaces with Istio injection:"
          kubectl get namespaces -L istio-injection

      - name: 🔒 Configure mTLS
        if: github.event.inputs.enable_mtls == 'true'
        run: |
          echo "🔒 Configuring PERMISSIVE mTLS (allows external Ingress traffic)..."
          
          cat <<EOF | kubectl apply -f -
          apiVersion: security.istio.io/v1beta1
          kind: PeerAuthentication
          metadata:
            name: default-mtls-permissive
            namespace: aks-istio-system
          spec:
            mtls:
              mode: PERMISSIVE
          ---
          apiVersion: security.istio.io/v1beta1
          kind: PeerAuthentication
          metadata:
            name: dx02-mtls-permissive
            namespace: dx02
          spec:
            mtls:
              mode: PERMISSIVE
          EOF
          
          echo "✅ mTLS configured successfully (PERMISSIVE mode - allows Nginx Ingress)"

      - name: 🌐 Deploy Istio Ingress Gateway
        if: github.event.inputs.enable_ingress_gateway == 'true'
        continue-on-error: true
        run: |
          echo "🌐 Note: Azure Service Mesh uses service-specific Gateways"
          echo "Instead of a central Ingress Gateway, each application defines its own Gateway resource"
          echo "✅ No separate Ingress Gateway deployment needed for Azure Service Mesh"
          echo ""
          echo "Gateway will be created with your application deployment using:"
          echo "  apiVersion: networking.istio.io/v1beta1"
          echo "  kind: Gateway"
          echo ""
          echo "The Gateway will automatically create a LoadBalancer service"
          echo "✅ Ingress Gateway configuration skipped (not needed for Azure Service Mesh)"

      - name: 📊 Configure Telemetry
        if: github.event.inputs.enable_telemetry == 'true'
        run: |
          echo "📊 Telemetry configuration..."
          echo "⚠️ Note: Azure Service Mesh has built-in telemetry integration."
          echo "Telemetry is automatically configured with Azure Monitor and Prometheus."
          echo "Custom Telemetry resources (v1alpha1) are not supported by Azure Service Mesh."
          echo "✅ Using default Azure Service Mesh telemetry (no custom config needed)"

      - name: 🎯 Deploy DX02 Gateway and VirtualService
        run: |
          echo "🎯 Deploying Gateway and VirtualService for DX02..."
          
          cat <<EOF | kubectl apply -f -
          apiVersion: networking.istio.io/v1beta1
          kind: Gateway
          metadata:
            name: dx02-gateway
            namespace: dx02
          spec:
            selector:
              istio: ingressgateway
            servers:
            - port:
                number: 80
                name: http
                protocol: HTTP
              hosts:
              - "*"
          ---
          apiVersion: networking.istio.io/v1beta1
          kind: VirtualService
          metadata:
            name: dx02-vs
            namespace: dx02
          spec:
            hosts:
            - "*"
            gateways:
            - dx02-gateway
            http:
            - match:
              - uri:
                  prefix: "/"
              route:
              - destination:
                  host: dx02-service
                  port:
                    number: 80
              timeout: 30s
              retries:
                attempts: 3
                perTryTimeout: 10s
          EOF
          
          echo "✅ Gateway and VirtualService deployed"

      - name: 🔍 Verify complete installation
        run: |
          echo "======================================"
          echo "🕸️ Azure Service Mesh Status"
          echo "======================================"
          echo ""
          
          echo "📦 Istio System Pods:"
          kubectl get pods -n aks-istio-system
          echo ""
          
          echo "🌐 Ingress Gateway:"
          kubectl get svc istio-ingressgateway -n aks-istio-ingress 2>/dev/null || echo "Not deployed"
          echo ""
          
          echo "🏷️ Namespaces with Istio injection:"
          kubectl get namespaces -L istio-injection | grep enabled
          echo ""
          
          echo "🔒 Security Policies:"
          kubectl get peerauthentication --all-namespaces
          echo ""
          
          echo "🎯 Gateways and VirtualServices:"
          kubectl get gateway,virtualservice -n dx02
          echo ""
          
          echo "✅ Azure Service Mesh installation complete!"

      - name: 📝 Generate configuration summary
        run: |
          cat > service-mesh-summary.txt <<EOF
          ====================================
          Azure Service Mesh Configuration
          ====================================
          
          Environment: ${{ github.event.inputs.environment }}
          AKS Cluster: ${{ env.AKS_CLUSTER_NAME }}
          Resource Group: ${{ env.AZURE_RESOURCE_GROUP }}
          Istio Version: ${{ env.ISTIO_VERSION }}
          
          Features Enabled:
          - mTLS: ${{ github.event.inputs.enable_mtls }}
          - Telemetry: ${{ github.event.inputs.enable_telemetry }}
          - Ingress Gateway: ${{ github.event.inputs.enable_ingress_gateway }}
          
          Note: Azure Service Mesh uses Gateway resources per application.
          External IP will be available after deploying application Gateway.
          EOF
          
          echo ""
          cat service-mesh-summary.txt

      - name: 📤 Upload summary
        uses: actions/upload-artifact@v4
        with:
          name: service-mesh-summary-${{ github.event.inputs.environment }}
          path: service-mesh-summary.txt
          retention-days: 30

  validate-service-mesh:
    name: ✅ Validate Service Mesh
    needs: install-service-mesh
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: 🔐 Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 🎯 Get AKS credentials
        run: |
          az aks get-credentials \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --name ${{ env.AKS_CLUSTER_NAME }} \
            --overwrite-existing

      - name: 🔍 Validate Istio components
        run: |
          echo "🔍 Running validation tests..."
          
          # Check Istio system pods are running
          echo "✓ Checking Istio system pods..."
          kubectl get pods -n aks-istio-system -o wide
          
          # Check all pods are ready
          NOT_READY=$(kubectl get pods -n aks-istio-system --field-selector=status.phase!=Running -o name | wc -l)
          if [ $NOT_READY -gt 0 ]; then
            echo "❌ Some Istio pods are not ready"
            exit 1
          fi
          echo "✅ All Istio system pods are ready"
          
          # Check sidecar injection
          echo ""
          echo "✓ Checking Istio sidecar injection..."
          INJECTED_NS=$(kubectl get namespaces -l istio-injection=enabled -o name | wc -l)
          echo "✅ Found $INJECTED_NS namespaces with Istio injection enabled"
          
          # Validate mTLS configuration
          if [ "${{ github.event.inputs.enable_mtls }}" == "true" ]; then
            echo ""
            echo "✓ Validating mTLS configuration..."
            kubectl get peerauthentication --all-namespaces
            echo "✅ mTLS policies configured"
          fi
          
          # Check Ingress Gateway
          if [ "${{ github.event.inputs.enable_ingress_gateway }}" == "true" ]; then
            echo ""
            echo "✓ Note: Azure Service Mesh uses per-application Gateway resources"
            echo "✅ Gateway will be created with application deployment"
          fi
          
          echo ""
          echo "======================================"
          echo "✅ All validations passed!"
          echo "======================================"

      - name: 🎉 Success notification
        run: |
          echo "======================================"
          echo "🎉 Service Mesh Deployment Complete!"
          echo "======================================"
          echo ""
          echo "Azure Service Mesh (Istio) has been successfully"
          echo "deployed and configured on AKS cluster:"
          echo "${{ env.AKS_CLUSTER_NAME }}"
          echo ""
          echo "Next steps:"
          echo "1. Restart pods in enabled namespaces to inject sidecars"
          echo "2. Configure traffic management policies"
          echo "3. Set up monitoring dashboards"
          echo "4. Review security policies"
          echo ""
          echo "Documentation: SERVICE_MESH.md"
