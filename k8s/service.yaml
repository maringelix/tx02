apiVersion: v1
kind: Service
metadata:
  name: dx02
  labels:
    app: dx02
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: dx02
